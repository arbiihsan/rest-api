apiVersion: v1
kind: Service
metadata:
  name: rest-api-service
  labels:
    app: rest-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: rest-api
